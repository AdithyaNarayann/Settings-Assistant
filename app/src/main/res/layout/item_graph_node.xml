<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nodeCardContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="4dp"
    android:paddingEnd="8dp"
    android:paddingTop="5dp"
    android:paddingBottom="5dp">

    <!-- Main node row -->
    <LinearLayout
        android:id="@+id/nodeRowContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Tree branch indentation & connector -->
        <TextView
            android:id="@+id/treeBranchPrefix"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#9E9E9E"
            android:fontFamily="monospace"
            tools:text="    └─ " />

        <TextView
            android:id="@+id/nodeIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:layout_marginEnd="6dp"
            tools:text="📁" />

        <TextView
            android:id="@+id/nodeLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="15sp"
            android:textColor="#212121"
            tools:text="Display &amp; brightness" />

        <!-- Depth Badge -->
        <TextView
            android:id="@+id/depthBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textStyle="bold"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textColor="#1565C0"
            android:background="#E3F2FD"
            tools:text="ROOT" />

    </LinearLayout>

    <!-- Path / Location breadcrumb -->
    <TextView
        android:id="@+id/nodeLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="#546E7A"
        android:background="#ECEFF1"
        android:paddingStart="6dp"
        android:paddingEnd="6dp"
        android:paddingTop="1dp"
        android:paddingBottom="1dp"
        android:layout_marginStart="24dp"
        android:layout_marginTop="2dp"
        android:visibility="gone"
        tools:text="📍 Display &amp; brightness > Font size"
        tools:visibility="visible" />

    <!-- Subtitle / summary if present -->
    <TextView
        android:id="@+id/nodeSubtitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#757575"
        android:layout_marginStart="24dp"
        android:layout_marginTop="1dp"
        android:visibility="gone"
        tools:text="Eye Protection, Dark theme, status bar"
        tools:visibility="visible" />

</LinearLayout>
